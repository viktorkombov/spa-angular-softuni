import { ShortenTextForRecipeItemPipe } from './shorten-text-for-recipe-item.pipe';

describe('ShortenTextForRecipeItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenTextForRecipeItemPipe();
    expect(pipe).toBeTruthy();
  });
});
